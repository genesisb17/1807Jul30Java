package com.revature.oop;

// a class cannot be both abstract and final
// abstract means a class must be extended
// final means you cannot extend a class

public abstract class Animal implements Livable {
	
	/* 
	 * Abstract classes have the ability to have abstract methods (unimplemented
	 * methods). They do NOT need to have an abstract method to be abstract. They
	 * just have the ability to
	 */
	
	/* because Animal is abstract, we don't have to implement 
	 * all of Livable's methods here
	 */

}
