<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd">
	<context:annotation-config />
	<context:component-scan
		base-package="com.iantimothyjohnson.notes.week6" />

	<mvc:annotation-driven />
	<!-- Here's where we specify where our resources are. -->
	<mvc:resources location="/static" mapping="/**" />

	<!-- View resolver: we will access the InternalResourceViewResolver. -->
	<bean id="viewResolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- This is where we specify the directory containing our static resources. -->
		<property name="prefix" value="static/" />
		<!-- This is the suffix of all the resources we want to return. -->
		<property name="suffix" value=".html" />
	</bean>
</beans>
